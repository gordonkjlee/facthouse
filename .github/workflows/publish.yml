name: Publish to npm

on:
  workflow_run:
    # Must match the `name:` in release-please.yml exactly
    workflows: ["Release Please"]
    types: [completed]
  workflow_dispatch:

concurrency:
  group: publish-npm
  cancel-in-progress: false

jobs:
  publish:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    environment: npm
    steps:
      - name: Find unpublished release
        id: release
        run: |
          TAG=$(gh api repos/${{ github.repository }}/releases/latest --jq '.tag_name' 2>/dev/null || echo "")
          if [ -z "$TAG" ]; then
            echo "No releases found"
            echo "found=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          VERSION="${TAG#v}"
          if npm view @openmem/mcp@"$VERSION" version 2>/dev/null; then
            echo "$TAG already on npm"
            echo "found=false" >> "$GITHUB_OUTPUT"
          else
            echo "$TAG not on npm — publishing"
            echo "found=true" >> "$GITHUB_OUTPUT"
            echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - if: steps.release.outputs.found == 'true'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ steps.release.outputs.tag }}

      - if: steps.release.outputs.found == 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      # Trusted publishing (OIDC) requires npm >= 11.5.1; Node 22 ships npm 10.x,
      # so npm must be upgraded explicitly.
      #
      # Pinned to the 11.x line, NOT @latest. npm 12 (2026-07-08) stopped running
      # dependency lifecycle scripts unless explicitly allowlisted, which silently
      # skips better-sqlite3's prebuild-install — leaving no native binary, so the
      # test suite's SQLite tests all skip and the CLI exits non-zero. 11.x gets
      # OIDC without that behaviour change.
      - if: steps.release.outputs.found == 'true'
        name: Upgrade npm for trusted publishing
        run: |
          npm install -g "npm@^11.5.1"
          npm --version

      - if: steps.release.outputs.found == 'true'
        name: Install dependencies
        run: npm install  # `prepare` script runs tsc after install

      - if: steps.release.outputs.found == 'true'
        name: Run tests
        run: npm test

      # No NODE_AUTH_TOKEN: authentication comes from the OIDC id-token above,
      # matched against the Trusted Publisher configured on npmjs.com for this
      # package (owner/repo + workflow filename + environment must match exactly).
      # Provenance is generated automatically under trusted publishing.
      - if: steps.release.outputs.found == 'true'
        name: Publish ${{ steps.release.outputs.tag }}
        run: npm publish --provenance --access public
